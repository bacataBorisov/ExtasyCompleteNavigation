# macOS

.DS_Store

# Logs


# Build files

.build/
DerivedData/

# Agent OS: only `.agent-os/config.json` is tracked. All other `.agent-os/*` output is
# local (gitignored). `agentos cache update` updates disk but not git — see `.agent/DOCUMENTATION.md`.

.agent-os/data/
.agent-os/context/
.agent-os/exports/
.agent-os/logs/
.agent-os/state/

# Secrets

# Additional files

project-structure.txt
*.xcuserstate

# Jupyter / scratch (keep repo scans and git history clean)
Untitled.ipynb
.ipynb_checkpoints/
.virtual_documents/
