{
  "version": 1,
  "chunk_lines": 100,
  "exclude_dirs": [
    ".git",
    "node_modules",
    ".agent-os",
    ".build",
    "DerivedData",
    ".ipynb_checkpoints",
    ".virtual_documents"
  ],
  "notes": "Exclude .build/DerivedData and Jupyter scratch dirs. Generated .agent-os/* (except this file) is gitignored — see .agent/DOCUMENTATION.md §3. If handoff still lists .build/*, delete local .build/ and run `agentos init .` from repo root."
}
